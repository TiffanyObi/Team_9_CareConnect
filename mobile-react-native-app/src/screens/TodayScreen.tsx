import React from 'react'; import { Alert, ScrollView, StyleSheet, View } from 'react-native'; import { BottomTabScreenProps } from '@react-navigation/bottom-tabs'; import { useApp } from '../context/AppContext'; import { AppText, Button, Card, Screen } from '../components/UI'; import { TabParams } from '../navigation/RootNavigator';
export function TodayScreen({ navigation }: BottomTabScreenProps<TabParams, 'Today'>): React.JSX.Element { const { userName, signOut } = useApp(); return <Screen><ScrollView contentContainerStyle={s.content}><View style={s.header}><AppText style={s.logo}>CareConnect</AppText><Button label="Log out" secondary onPress={() => Alert.alert('Log out?', 'Are you sure you want to log out?', [{ text: 'Cancel' }, { text: 'Log out', onPress: signOut }])} /></View><AppText heading>Good morning, {userName}</AppText><AppText secondary>Tuesday, August 31 • 2 tasks remaining</AppText><Card tone="warning" label="Medication due at 8 AM. Levetiracetam. Take with water."><AppText style={[s.cardTitle, s.darkText]}>Medication due at 8:00 AM</AppText><AppText style={s.darkText}>Levetiracetam • Take with water</AppText></Card><Button label="Log medication" onPress={() => navigation.navigate('Medications')} /><Card><AppText style={s.cardTitle}>Physical therapy</AppText><AppText>3:30 PM • Northside Clinic</AppText></Card><Card><AppText style={s.cardTitle}>Caregiver connection</AppText><AppText>Mom can view medication status, not private notes.</AppText></Card></ScrollView></Screen>; }
const s = StyleSheet.create({ content: { padding: 20, paddingBottom: 32, gap: 12, maxWidth: 760, width: '100%', alignSelf: 'center' }, header: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' }, logo: { fontSize: 21, fontWeight: '800', color: '#0B7F79' }, cardTitle: { fontWeight: '700', fontSize: 18 }, darkText: { color: '#10233F' } });
